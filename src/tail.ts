/**
 * @description Find the tail elements following the head.
 * @param {*[]} collection
 * @returns {*[]} Tail elements.
 * @see #head
 */
export default function tail([, ...elements]: any[]): any[] {
  return elements
}
